import React from "react";

import "./result.css";

const Result = ({ country}) => (
  <div>
    <div className="country">
      <label>{country}</label>
        </div>
  </div>
);

export default Result;